version: "3.9"

services:
  bundler:
    container_name: bundler-scroll-mainnet-v1.2
    build: .
    restart: unless-stopped
    ports:
      - 10201:4337
    volumes:
      - .env-scroll-mainnet:/app/.env
    environment:
      - TZ=Asia/Shanghai
